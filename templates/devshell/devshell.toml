[devshell]
name = "devshell"

# One nixpkgs attribute per entry, e.g. ["postgresql", "jq"].
packages = []

# Named commands show up in the `menu` printed on entry:
# [[commands]]
# name = "psql"
# help = "PostgreSQL interactive terminal"
# category = "database"
# package = "postgresql"
